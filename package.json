{
  "name": "yurttas-mutfakta-root",
  "private": true,
  "scripts": {
    "postinstall": "cd frontend && bun install",
    "dev": "cd frontend && bun run dev",
    "build": "cd frontend && bun install && bun run build",
    "build:dev": "cd frontend && bun install && bun run build:dev",
    "preview": "cd frontend && bun run preview",
    "lint": "cd frontend && bun run lint"
  }
}
